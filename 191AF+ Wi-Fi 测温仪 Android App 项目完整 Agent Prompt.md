# 191AF+ Wi-Fi 测温仪 Android App 项目 —— Agent 完整任务 Prompt

你现在负责协助我完成一个工业现场 Android App 项目。

## 一、项目背景

我有一台 191AF+ 系列测温仪，需要开发一个 Android 平板 App，使 Android 平板能够通过 Wi-Fi 与 191AF+ 测温仪通信。

目前厂家只提供了两份 PDF 文档：

1. `191AF 系列通讯协议 V1.1`
2. `191AF/192AF 测试仪通讯示例`

请严格以这两份文档作为当前项目的主要协议依据。

**非常重要：**

- 不要凭空假设厂家没有提供的信息。
- 不要自行发明不存在的寄存器。
- 如果文档没有说明某个行为，要明确标记为“待厂家确认”。
- 可以使用通用的 Android / Kotlin / TCP / MODBUS 技术知识进行方案设计，但必须明确区分：
  - 厂家文档明确说明的内容
  - 根据厂家协议做出的合理推断
  - Android 技术实现方案
  - 目前未知、需要实际测试或厂家确认的内容

---

# 二、目前已经确认的厂家协议

## 2.1 MODBUS RTU

厂家文档说明：

- 采用 RS485
- MODBUS-RTU
- N-8-1
- 无奇偶校验
- 8 位数据位
- 1 位停止位
- 波特率 115200
- 默认设备地址为 `0x01`

但是本项目主要采用 Wi-Fi，因此重点不是 RTU，而是 MODBUS TCP/IP。

---

# 三、191AF+ 的 MODBUS TCP/IP

厂家文档明确说明：

- 191AF+ 支持 MODBUS-TCP/IP
- 使用 2.4G Wi-Fi
- 测温仪硬件必须支持无线通讯
- MODBUS TCP 报文格式：
  `MBAP Header + Function Code + Data`
- 无线协议设置为：
  `MB-TCP`
- 默认端口：
  `502`
- 网络连接成功后：
  **上位机主动发送 MODBUS 读/写请求，测温仪收到请求后回复**
- 厂家建议巡检周期约为：
  **1 秒**
- 如果通信间隔超过约 60 秒，测温仪可能认为通信超时。

因此，本项目计划：

```text
191AF+
   │
   │ Wi-Fi 2.4GHz
   ▼
工厂 Wi-Fi 网络
   │
   ▼
Android 平板
   │
   │ MODBUS TCP
   ▼
191AF+
```

目标是一台 191AF+ 测温仪对应一台 Android 平板。

---

# 四、目前厂家文档明确给出的寄存器

根据 `191AF 系列通讯协议 V1.1`，目前可以确认：

| 地址 | 内容 | 说明 | R/W |
|---|---|---|---|
| `0x00` | 实时温度 | 单位 ℃，系数 0.1 | R |
| `0x01` | 温度单位 | 0=℃，1=℉ | R/W |
| `0x02` | 结果保存 | 0=手动保存，1=自动保存 | R/W |
| `0x03` | 自动关机时间 | 0、5、10、15 分钟 | R/W |
| `0x04~0x09` | 备用 | 当前不用 | — |
| `0x0A~0x19` | 设备信息 | 字符串，按字节解析 | R |
| `0x1A` | 设定温度 | 单位 ℃，例如 350 | R |
| `0x1B` | 误差范围 | 单位 ℃，例如 10 表示 ±10℃ | R |
| `0x1C` | 测试上传标志 | 0=无效，1=保存按钮按下；读取后自动清零 | R |
| `0x1D` | 测试保存温度 | 单位 ℃；测试上传标志为 1 时有效 | R |
| `0x1E` | 测试合格判定 | 0=NG，1=OK；测试上传标志为 1 时有效 | R |

厂家特别说明：

- `0x1C` 是测试上传标志
- `0x1C = 1` 表示保存按钮按下
- 读取 `0x1C` 后自动清零
- `0x1D` 是保存温度
- `0x1E` 是测试合格判定
- `0x1D` 和 `0x1E` 在 `0x1C = 1` 时有效

---

# 五、非常重要：不要把当前寄存器表称为“完整寄存器表”

目前只能确定厂家文档明确提供了上述地址。

不能自行假设：

```text
0x1F
0x20
0x21
...
```

是什么。

如果需要完整的 191AF+ Register Map，应该把：

**“请厂家提供完整 MODBUS-TCP/IP 寄存器地址表 / Register Map”**

列为待确认事项。

---

# 六、实际测试流程

目前已知的实际使用流程大概是：

1. 操作员使用 191AF+ 测量。
2. 测量完成后，操作员按下 191AF+ 设备上的“保存”按钮。
3. 仪器保存一笔测试结果。
4. Android App 通过 Wi-Fi 与仪器通信。
5. App 周期性轮询仪器。
6. App 读取 `0x1C`。
7. 如果 `0x1C = 0`，说明目前没有新的测试结果。
8. 如果 `0x1C = 1`，说明保存按钮被按下，有一笔新的测试结果。
9. App 读取：
   - `0x1D`：保存温度
   - `0x1E`：OK/NG
10. App 自动生成一条测量记录。
11. App 将记录保存到 Android 本地数据库。

---

# 七、关于“是否需要手动触发”的核心要求

这是本项目非常重要的要求：

## 我不希望操作员在 App 上再次点击“保存”。

正确的交互应该是：

```text
操作员
  ↓
使用 191AF+ 测量
  ↓
按下 191AF+ 自己的保存按钮
  ↓
191AF+ 产生测试结果
  ↓
Android App 自动轮询
  ↓
发现 0x1C = 1
  ↓
自动读取 0x1D
  ↓
自动读取 0x1E
  ↓
自动保存到数据库
```

也就是说：

**仪器上的保存按钮是测试完成触发源，App 只是自动采集。**

App 不应该要求操作员再点击一次保存。

---

# 八、但是必须验证 0x1C / 0x1D / 0x1E 的真实行为

目前厂家文档虽然说明：

```text
0x1C = 1 → 保存按钮按下
0x1D → 保存温度
0x1E → OK/NG
```

但我们没有实际通信测试结果。

因此你需要设计一个“通信验证阶段”。

需要验证：

### 测试 A

设备正常连接后：

```text
0x1C = ?
0x1D = ?
0x1E = ?
```

### 测试 B

不进行测量、不按保存：

持续读取：

```text
0x1C
```

确认是否一直为 0。

### 测试 C

完成一次测量并按设备保存按钮：

立即读取：

```text
0x1C
0x1D
0x1E
```

记录结果。

### 测试 D

读取 `0x1C` 后再次读取：

确认它是否真的自动变成 0。

### 测试 E

连续进行两次测试：

```text
第一次：
保存 → 读取结果

第二次：
保存 → 读取结果
```

确认不会丢数据。

### 测试 F

App 轮询过程中按下保存按钮：

确认 App 是否能自动发现。

### 测试 G

App 暂时断开 Wi-Fi，然后仪器完成一次保存，再恢复 Wi-Fi：

验证：

- 结果是否还能读取
- `0x1C` 是否已经丢失
- 是否存在数据丢失风险

如果无法实际连接仪器进行测试，不要假设结果。

---

# 九、Android App 功能需求

目标平台：

**Android 平板**

设备数量：

**一台 191AF+ + 一台 Android 平板**

Wi-Fi：

**使用工厂现有 Wi-Fi**

通信：

**MODBUS TCP/IP**

默认：

```text
Port = 502
Unit ID = 1
```

但允许在配置中修改。

---

# 十、App 页面设计

建议至少设计以下页面。

## 10.1 测量页面

主要页面。

包含：

```text
线别
机种

当前温度

通信状态

测试状态

最后一次测试结果
```

例如：

```text
--------------------------------
191AF+ 测量系统

线别
[ L01              ▼ ]

机种
[ ABC-001          ▼ ]

当前温度
351.0 ℃

设备状态
● 已连接

测试状态
等待测试...

最近结果
351 ℃
OK

时间
2026-09-07 22:31:05
--------------------------------
```

当仪器保存按钮被按下：

```text
测试完成

351.0 ℃

✓ OK

已自动保存
```

如果 NG：

```text
测试完成

362.0 ℃

✕ NG

已自动保存
```

---

# 十一、线别功能

App 必须支持：

- 线别列表
- 新增线别
- 修改线别
- 删除线别
- 搜索线别
- 下拉选择

例如：

```text
L01
L02
L03
L04
L05
```

---

# 十二、机种功能

App 必须支持：

- 机种列表
- 新增机种
- 修改机种
- 删除机种
- 搜索机种
- 下拉选择

而且下拉框必须支持：

**输入文字实时过滤。**

例如机种很多：

```text
ABC-001
ABC-002
ABC-003
ABC-100
ABC-101
XYZ-001
XYZ-002
```

输入：

```text
ABC-1
```

自动只显示匹配结果。

---

# 十三、配置中心

需要一个独立的“配置”页面。

至少包含：

## 仪器通信配置

```text
IP 地址
Port
Unit ID
连接超时时间
轮询周期
```

默认：

```text
Port = 502
Unit ID = 1
Poll Interval = 1000ms
```

这些配置必须持久化保存。

用户配置一次后，下次打开 App 自动读取。

---

# 十四、配置文件

我希望设计一个专门的配置机制。

至少可以保存：

```text
设备通信配置
线别列表
机种列表
```

例如可以设计成：

```text
config.json
```

或者 Android 本地数据库 + JSON 导入/导出。

你需要分析哪一种更适合。

最好支持：

```text
导出配置
导入配置
```

这样以后可以把一台平板配置好的内容复制到另一台平板。

但是：

**配置文件设计不能影响正常数据库存储。**

需要把“配置数据”和“测量历史数据”分开。

---

# 十五、测量数据

每一次测试成功后自动保存。

建议至少保存以下字段：

```text
id
timestamp
line
model
device_id
device_ip
set_temperature
measured_temperature
tolerance
result
```

例如：

```text
ID：000001

时间：
2026-09-07 22:31:05

线别：
L01

机种：
ABC-001

设备：
191AF+

设备 IP：
192.168.1.100

设定温度：
350℃

测量温度：
351℃

误差范围：
±10℃

结果：
OK
```

---

# 十六、数据库

需要设计 Android 本地数据库。

优先分析：

**Room + SQLite**

是否合适。

数据库至少应该包含：

### MeasurementRecord

```text
id
timestamp
lineId / lineName
modelId / modelName
deviceId
deviceIp
setTemperature
measuredTemperature
tolerance
result
```

### Line

```text
id
name
enabled
createdAt
updatedAt
```

### Model

```text
id
name
enabled
createdAt
updatedAt
```

### DeviceConfig

```text
id
name
ip
port
unitId
pollInterval
enabled
```

如果你认为还需要其他表，可以提出。

---

# 十七、历史记录

需要一个历史数据页面。

支持：

- 查看所有记录
- 按日期筛选
- 按线别筛选
- 按机种筛选
- 按 OK/NG 筛选
- 关键词搜索
- 查看详细记录

例如：

```text
2026-09-07

09:01:12
L01 / ABC-001
351℃ / OK

09:03:25
L01 / ABC-001
362℃ / NG

09:05:31
L01 / ABC-001
350℃ / OK
```

---

# 十八、Excel 导出

必须支持：

**导出 Excel 文件。**

例如：

```text
测量记录_20260907.xlsx
```

Excel 字段：

```text
ID
时间
线别
机种
设备
设备IP
设定温度
测量温度
误差范围
结果
```

最好支持导出前筛选：

```text
日期范围
线别
机种
结果
```

例如：

```text
2026/09/01 ～ 2026/09/07
线别：L01
机种：ABC-001
结果：全部
```

然后导出。

---

# 十九、通信模块设计

请把 MODBUS TCP 通信独立成一个模块。

不要把 Socket 代码直接写在 Activity / UI 中。

推荐架构：

```text
UI
 ↓
ViewModel
 ↓
Repository
 ↓
ModbusService
 ↓
TCP Socket
 ↓
191AF+
```

通信模块负责：

```text
connect()
disconnect()

readHoldingRegisters()
writeSingleRegister()
writeMultipleRegisters()

readRealtimeTemperature()
readUploadFlag()
readSavedTemperature()
readTestResult()
```

---

# 二十、轮询逻辑

核心逻辑：

```text
连接仪器
 ↓
每约 1 秒执行一次
 ↓
读取 0x1C
 ↓
0 → 无新测试
 ↓
1 → 有新测试
 ↓
读取 0x1D
 ↓
读取 0x1E
 ↓
创建 MeasurementRecord
 ↓
写入 Room
 ↓
更新 UI
```

需要考虑：

- TCP 断线
- Wi-Fi 暂时断开
- 仪器关机
- 仪器 IP 不可达
- Socket timeout
- 数据解析错误
- MODBUS exception response
- CRC 是否需要（注意：MODBUS TCP 不使用 RTU CRC）
- 读取失败重试
- App 从后台恢复
- Android 生命周期
- 防止重复保存

---

# 二十一、特别注意“重复保存”

因为：

```text
0x1C = 1
```

并且：

**读取以后自动清零。**

因此设计上需要避免：

```text
读取 0x1C = 1
读取 0x1D
读取 0x1E
数据库保存
```

过程中 App 崩溃或者网络异常造成重复/丢失。

需要分析：

**什么时候认为一次测试已经成功确认？**

并设计合理的事务/状态机。

---

# 二十二、请设计状态机

建议分析并设计类似：

```text
DISCONNECTED
      ↓
CONNECTING
      ↓
CONNECTED
      ↓
POLLING
      ↓
NEW_RESULT_DETECTED
      ↓
READING_RESULT
      ↓
SAVING
      ↓
SAVED
      ↓
POLLING
```

异常：

```text
CONNECTED
   ↓
NETWORK_ERROR
   ↓
RECONNECTING
   ↓
CONNECTED
```

请详细说明状态转换条件。

---

# 二十三、MODBUS TCP 报文

请根据厂家 PDF 给出的示例，设计实际 App 使用的请求。

厂家示例：

请求：

```text
00 01 00 00 00 06 01 03 00 00 00 0A
```

其中：

```text
Transaction ID = 0001
Protocol ID    = 0000
Length         = 0006
Unit ID        = 01
Function       = 03
Start Address  = 0000
Quantity       = 000A
```

响应示例：

```text
00 01 00 00 00 17 01 03 14 ...
```

厂家示例说明：

```text
寄存器 0 = 实时温度
例如：
00 EA
= 234
= 23.4℃
```

请根据这个规则设计：

### 读取 0x1C~0x1E

优先考虑一次读取：

```text
Start Address = 0x001C
Quantity = 3
```

即：

```text
01 03 00 1C 00 03
```

然后说明完整 MODBUS TCP MBAP Header 应该怎么组成。

不要只给结论，请解释字节结构。

---

# 二十四、数据类型和缩放

目前厂家文档说明：

实时温度：

```text
0x00
系数 0.1
```

例如：

```text
234 → 23.4℃
```

测试保存温度：

```text
0x1D
单位 ℃
```

例如：

```text
351 → 351℃
```

请严格区分：

```text
0x00 的缩放规则
0x1D 的缩放规则
```

不要把所有温度都默认乘 0.1。

---

# 二十五、网络配置

厂家提供了二维码配置方式。

文档中明确有：

```text
WifiName
WifiPassword
ServerIP
ServerPort
GateWay
```

例如：

```text
WifiName=Quick-service
WifiPassword=1921681711
ServerIP=192.168.170.100
ServerPort=1883
```

但是注意：

厂家文档特别说明：

**MQTT 和 MODBUS TCP 模式下 ServerIP 的含义不同。**

MQTT：

```text
ServerIP = 上位机 IP
```

MODBUS TCP：

```text
ServerIP = 测试仪 IP
```

本项目采用：

**MODBUS TCP**

所以不要把 MQTT 示例直接照搬。

请明确分析：

- 191AF+ 的 IP 应如何配置
- Android 平板如何连接同一个工厂 Wi-Fi
- Android App 如何连接仪器 IP:502
- DHCP / 静态 IP 的优缺点
- 工厂网络环境下可能存在的 AP isolation / VLAN / 防火墙问题

但不要改变项目当前目标：

**一台仪器 + 一台 Android 平板 + 工厂 Wi-Fi。**

---

# 二十六、UI/UX 原则

这是一个工业现场 App。

因此优先：

- 大字体
- 清晰状态
- 简单操作
- 高可读性
- 少按钮
- 明确 OK / NG
- 网络状态明显
- 测试完成明显
- 避免误操作

操作员的主要操作应该只有：

```text
选择线别
选择机种
进行测量
按仪器保存按钮
```

App 自动完成：

```text
读取
判断新数据
获取结果
保存
显示
```

---

# 二十七、推荐技术栈

请评估并给出最终建议。

默认优先考虑：

```text
Kotlin
Android
Jetpack Compose
MVVM
Coroutines
StateFlow
Room
Apache POI / Android 适合的 Excel 生成方案
```

但对于 Excel 导出，请认真评估：

- Apache POI
- FastExcel
- 其他 Android 兼容方案

选择一个最适合本项目的。

MODBUS TCP：

可以自行基于：

```text
java.net.Socket
```

实现简单 MODBUS TCP Client，

或者使用成熟库。

请比较：

**自己实现 vs 使用第三方 MODBUS 库**

并选择适合工业项目长期维护的方案。

---

# 二十八、请先不要马上写完整 App

你的第一阶段任务不是立即生成完整代码。

请先完成：

## Phase 1：协议分析

输出：

1. 当前两份 PDF 的协议总结
2. 191AF+ 已知寄存器表
3. 哪些寄存器是 App 必须读取的
4. MODBUS TCP 报文结构
5. 目前协议未知项
6. 必须向厂家确认的问题

---

# 二十九、Phase 2：通信验证工具

设计一个最小 Android 测试 App。

只实现：

```text
IP
Port
Unit ID

连接

实时读取：
0x00
0x1A
0x1B
0x1C
0x1D
0x1E

自动轮询

通信日志
```

日志例如：

```text
[22:31:01] CONNECT 192.168.1.100:502
[22:31:02] READ 0x001C~0x001E
[22:31:02] 0x1C=0
[22:31:03] READ 0x001C~0x001E
[22:31:03] 0x1C=1
[22:31:03] 0x1D=351
[22:31:03] 0x1E=1
[22:31:03] RESULT = OK
```

这个工具用于现场验证真实设备行为。

---

# 三十、Phase 3：正式 App

确认通信以后再实现：

```text
测量
历史记录
配置
线别管理
机种管理
数据库
Excel 导出
配置导入/导出
```

---

# 三十一、Phase 4：异常测试

必须测试：

### 网络

- Wi-Fi 断开
- Wi-Fi 恢复
- 仪器关机
- 仪器重新开机
- IP 错误
- Port 错误
- TCP timeout

### 测试流程

- 正常 OK
- 正常 NG
- 连续快速保存
- 长时间运行
- App 切后台
- App 恢复
- Android 平板重启

### 数据

- 数据重复
- 数据丢失
- 数据库写入失败
- Excel 导出失败
- 配置导入失败

---

# 三十二、必须重点分析的数据丢失风险

请认真分析：

假设：

```text
仪器：
0x1C = 1
0x1D = 351
0x1E = 1
```

App 读取 0x1C 后，仪器自动清零。

但此时：

```text
App 在读取 0x1D 前崩溃
```

会发生什么？

是否可能导致：

```text
0x1C 已清零
但 App 没有保存记录
```

如果存在这个风险，请明确指出。

并提出：

- 是否应该一次读取 `0x1C~0x1E`
- 是否应该一次 MODBUS 请求读取
- 是否应该先读取 0x1D/0x1E
- 如何减少失败窗口
- 是否需要本地 pending 状态
- 哪些问题必须让厂家确认

不要为了“看起来完整”而假装这个问题已经解决。

---

# 三十三、请考虑“自动获取结果”的可靠性

本项目的核心业务要求：

> 操作员只按仪器上的保存按钮，App 自动获取结果并保存。

因此请重点分析：

```text
仪器保存
↓
0x1C
↓
App Poll
↓
读取结果
↓
数据库
```

这个链路是否可靠。

如果厂家协议存在天然限制，要明确指出。

---

# 三十四、请设计最终系统架构

最终希望得到：

```text
┌─────────────────────────────┐
│         Android Tablet       │
│                             │
│  ┌───────────────────────┐  │
│  │       UI Layer        │  │
│  └──────────┬────────────┘  │
│             │               │
│  ┌──────────▼────────────┐  │
│  │      ViewModel        │  │
│  └──────────┬────────────┘  │
│             │               │
│  ┌──────────▼────────────┐  │
│  │      Repository       │  │
│  └───────┬────────┬──────┘  │
│          │        │         │
│  ┌───────▼───┐ ┌──▼──────┐  │
│  │ Modbus TCP│ │  Room   │  │
│  │  Service  │ │Database │  │
│  └───────┬───┘ └─────────┘  │
│          │                  │
└──────────┼──────────────────┘
           │
        Wi-Fi/TCP
           │
           ▼
      ┌───────────┐
      │  191AF+   │
      │ Measuring │
      │   Meter   │
      └───────────┘
```

---

# 三十五、最终请输出以下成果

请不要只给我一堆代码。

我要你按以下顺序完成：

## A. 项目可行性结论

明确回答：

**这个项目是否可以实现？**

重点回答：

> 测量完成、操作员按仪器保存按钮以后，Android App 是否可以自动得到结果？

---

## B. 协议分析

整理厂家 PDF 中确认的信息。

---

## C. 已知寄存器表

整理：

```text
地址
名称
数据类型
单位
缩放
读写
App用途
```

---

## D. 未知信息清单

列出所有不能从厂家 PDF 得出的信息。

---

## E. 厂家确认问题清单

帮我整理一份可以直接发给厂家的问题。

特别包括：

1. 191AF+ 是否确定支持 MODBUS TCP？
2. 默认 IP 获取方式？
3. 完整 Register Map？
4. 0x1C 的详细时序？
5. 0x1C 是否读取一次后立即清零？
6. 0x1D / 0x1E 在读取 0x1C 后是否仍保持有效？
7. 如果 App 未连接，仪器按保存按钮会不会丢数据？
8. 连续两次保存之间有没有最小时间间隔？
9. 0x1D 的单位和数据类型是否始终为整数 ℃？
10. 0x1E 的 OK/NG 是否完全由仪器最终判定？
11. 是否存在其他与测试结果相关的寄存器？
12. 是否支持静态 IP？
13. 是否支持 DHCP？
14. Wi-Fi 是否只支持 2.4GHz？
15. MODBUS TCP 默认 Unit ID 是否始终为 1？
16. 最大连接数是多少？
17. TCP 是否只允许一个客户端？
18. 通讯超时后的行为是什么？
19. 是否可以连续 1 秒轮询？
20. 如果设备进入省电/休眠状态，Wi-Fi/TCP 会如何处理？

---

# 三十六、不要做的事情

禁止：

1. 臆造寄存器。
2. 臆造厂家没有说明的数据结构。
3. 把 MQTT 协议和 MODBUS TCP 混淆。
4. 把 MODBUS RTU 和 MODBUS TCP 的帧格式混用。
5. 把 0x00 的 0.1 缩放规则错误应用到 0x1D。
6. 假设仪器会主动推送数据。
7. 假设 0x1C/0x1D/0x1E 的真实时序已经验证。
8. 在没有实际设备测试的情况下声称通信已经验证成功。
9. 一开始就生成大量无法验证的完整 App 代码。

---

# 三十七、重要开发原则

这是一个工业现场数据采集系统。

请优先考虑：

**可靠性 > 可维护性 > 简洁性 > UI 美观**

数据不能轻易丢失。

网络断开以后需要自动重连。

数据库必须本地保存。

App 不应该依赖云服务器才能正常工作。

Excel 导出应该使用本地数据。

配置应该本地持久化。

---

# 三十八、最终目标

最终我要得到一个 Android 平板 App：

```text
打开 App
   ↓
自动连接 191AF+
   ↓
选择线别
   ↓
选择机种
   ↓
开始工作
   ↓
操作员正常测量
   ↓
操作员按 191AF+ 保存按钮
   ↓
App 自动发现测试结果
   ↓
自动读取：
温度
OK/NG
   ↓
自动保存：
时间
线别
机种
设备
温度
结果
   ↓
历史记录可以查询
   ↓
最后可以筛选并导出 Excel
```

整个过程：

**操作员不需要在 App 上重复点击“保存”。**

---

# 三十九、你的第一步任务

现在请不要直接写完整 App。

先完成：

### 第一阶段：

**《191AF+ → Android App 技术可行性与通讯规格分析 V1.0》**

包括：

1. 可行性结论
2. 协议分析
3. 寄存器分析
4. MODBUS TCP 通讯流程
5. 自动获取结果流程
6. 数据库设计
7. App 架构
8. UI 页面规划
9. Excel 导出方案
10. 配置文件方案
11. 网络异常处理
12. 数据丢失风险
13. 通信验证方案
14. 厂家确认问题清单

完成第一阶段后，再进入：

**第二阶段：MODBUS TCP Android 通信测试工具设计与实现。**

然后才进入：

**第三阶段：正式生产 App。**

请始终记住：

**厂家 PDF 是协议事实来源；不确定的地方必须标记，不允许自行假设。**