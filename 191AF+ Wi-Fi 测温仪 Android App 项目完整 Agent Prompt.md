# 191AF+ Wi-Fi 测温仪 Android App 项目 —— Agent 完整任务 Prompt

你现在负责协助我完成一个工业现场 Android App 项目。

## 一、项目背景

我有一台 191AF+ 系列测温仪，需要开发一个 Android 平板 App，使 Android 平板能够通过 Wi-Fi 与 191AF+ 测温仪通信。

目前厂家只提供了两份 PDF 文档：

1. `191AF 系列通讯协议 V1.1`
2. `191AF/192AF 测试仪通讯示例`

请严格以这两份文档作为当前项目的主要协议依据。

**非常重要：**

- 不要凭空假设厂家没有提供的信息。
- 不要自行发明不存在的寄存器。
- 如果文档没有说明某个行为，要明确标记为“待厂家确认”。
- 可以使用通用的 Android / Kotlin / TCP / MODBUS 技术知识进行方案设计，但必须明确区分：
  - 厂家文档明确说明的内容
  - 根据厂家协议做出的合理推断
  - Android 技术实现方案
  - 目前未知、需要实际测试或厂家确认的内容

---

# 二、目前已经确认的厂家协议

## 2.1 MODBUS RTU

厂家文档说明：

- 采用 RS485
- MODBUS-RTU
- N-8-1
- 无奇偶校验
- 8 位数据位
- 1 位停止位
- 波特率 115200
- 默认设备地址为 `0x01`

但是本项目主要采用 Wi-Fi，因此重点不是 RTU，而是 MODBUS TCP/IP。

---

# 三、191AF+ 的 MODBUS TCP/IP

厂家文档明确说明：

- 191AF+ 支持 MODBUS-TCP/IP
- 使用 2.4G Wi-Fi
- 测温仪硬件必须支持无线通讯
- MODBUS TCP 报文格式：
  `MBAP Header + Function Code + Data`
- 无线协议设置为：
  `MB-TCP`
- 默认端口：
  `502`
- 网络连接成功后：
  **上位机主动发送 MODBUS 读/写请求，测温仪收到请求后回复**
- 厂家建议巡检周期约为：
  **1 秒**
- 如果通信间隔超过约 60 秒，测温仪可能认为通信超时。

因此，本项目计划：

```text
191AF+
   │
   │ Wi-Fi 2.4GHz
   ▼
工厂 Wi-Fi 网络
   │
   ▼
Android 平板
   │
   │ MODBUS TCP
   ▼
191AF+
```

目标是一台 191AF+ 测温仪对应一台 Android 平板。

---

# 四、目前厂家文档明确给出的寄存器

根据 `191AF 系列通讯协议 V1.1`，目前可以确认：

| 地址 | 内容 | 说明 | R/W |
|---|---|---|---|
| `0x00` | 实时温度 | 单位 ℃，系数 0.1 | R |
| `0x01` | 温度单位 | 0=℃，1=℉ | R/W |
| `0x02` | 结果保存 | 0=手动保存，1=自动保存 | R/W |
| `0x03` | 自动关机时间 | 0、5、10、15 分钟 | R/W |
| `0x04~0x09` | 备用 | 当前不用 | — |
| `0x0A~0x19` | 设备信息 | 字符串，按字节解析 | R |
| `0x1A` | 设定温度 | 单位 ℃，例如 350 | R |
| `0x1B` | 误差范围 | 单位 ℃，例如 10 表示 ±10℃ | R |
| `0x1C` | 测试上传标志 | 0=无效，1=保存按钮按下；读取后自动清零 | R |
| `0x1D` | 测试保存温度 | 单位 ℃；测试上传标志为 1 时有效 | R |
| `0x1E` | 测试合格判定 | 0=NG，1=OK；测试上传标志为 1 时有效 | R |

厂家特别说明：

- `0x1C` 是测试上传标志
- `0x1C = 1` 表示保存按钮按下
- 读取 `0x1C` 后自动清零
- `0x1D` 是保存温度
- `0x1E` 是测试合格判定
- `0x1D` 和 `0x1E` 在 `0x1C = 1` 时有效

---

# 五、非常重要：不要把当前寄存器表称为“完整寄存器表”

目前只能确定厂家文档明确提供了上述地址。

不能自行假设：

```text
0x1F
0x20
0x21
...
```

是什么。

如果需要完整的 191AF+ Register Map，应该把：

**“请厂家提供完整 MODBUS-TCP/IP 寄存器地址表 / Register Map”**

列为待确认事项。

---

# 六、实际测试流程

目前已知的实际使用流程大概是：

1. 操作员使用 191AF+ 测量。
2. 测量完成后，操作员按下 191AF+ 设备上的“保存”按钮。
3. 仪器保存一笔测试结果。
4. Android App 通过 Wi-Fi 与仪器通信。
5. App 周期性轮询仪器。
6. App 读取 `0x1C`。
7. 如果 `0x1C = 0`，说明目前没有新的测试结果。
8. 如果 `0x1C = 1`，说明保存按钮被按下，有一笔新的测试结果。
9. App 读取：
   - `0x1D`：保存温度
   - `0x1E`：OK/NG
10. App 自动生成一条测量记录。
11. App 将记录保存到 Android 本地数据库。

---

# 七、关于“是否需要手动触发”的核心要求

这是本项目非常重要的要求：

## 我不希望操作员在 App 上再次点击“保存”。

正确的交互应该是：

```text
操作员
  ↓
使用 191AF+ 测量
  ↓
按下 191AF+ 自己的保存按钮
  ↓
191AF+ 产生测试结果
  ↓
Android App 自动轮询
  ↓
发现 0x1C = 1
  ↓
自动读取 0x1D
  ↓
自动读取 0x1E
  ↓
自动保存到数据库
```

也就是说：

**仪器上的保存按钮是测试完成触发源，App 只是自动采集。**

App 不应该要求操作员再点击一次保存。

---

# 八、但是必须验证 0x1C / 0x1D / 0x1E 的真实行为

目前厂家文档虽然说明：

```text
0x1C = 1 → 保存按钮按下
0x1D → 保存温度
0x1E → OK/NG
```

但我们没有实际通信测试结果。

因此你需要设计一个“通信验证阶段”。

需要验证：

### 测试 A

设备正常连接后：

```text
0x1C = ?
0x1D = ?
0x1E = ?
```

### 测试 B

不进行测量、不按保存：

持续读取：

```text
0x1C
```

确认是否一直为 0。

### 测试 C

完成一次测量并按设备保存按钮：

立即读取：

```text
0x1C
0x1D
0x1E
```

记录结果。

### 测试 D

读取 `0x1C` 后再次读取：

确认它是否真的自动变成 0。

### 测试 E

连续进行两次测试：

```text
第一次：
保存 → 读取结果

第二次：
保存 → 读取结果
```

确认不会丢数据。

### 测试 F

App 轮询过程中按下保存按钮：

确认 App 是否能自动发现。

### 测试 G

App 暂时断开 Wi-Fi，然后仪器完成一次保存，再恢复 Wi-Fi：

验证：

- 结果是否还能读取
- `0x1C` 是否已经丢失
- 是否存在数据丢失风险

如果无法实际连接仪器进行测试，不要假设结果。

---

# 九、Android App 功能需求

目标平台：

**Android 平板**

设备数量：

**一台 191AF+ + 一台 Android 平板**

Wi-Fi：

**使用工厂现有 Wi-Fi**

通信：

**MODBUS TCP/IP**

默认：

```text
Port = 502
Unit ID = 1
```

但允许在配置中修改。

---

# 十、App 页面设计

建议至少设计以下页面。

## 10.1 测量页面

主要页面。

包含：

```text
线别
机种

当前温度

通信状态

测试状态

最后一次测试结果
```

例如：

```text
--------------------------------
191AF+ 测量系统

线别
[ L01              ▼ ]

机种
[ ABC-001          ▼ ]

当前温度
351.0 ℃

设备状态
● 已连接

测试状态
等待测试...

最近结果
351 ℃
OK

时间
2026-09-07 22:31:05
--------------------------------
```

当仪器保存按钮被按下：

```text
测试完成

351.0 ℃

✓ OK

已自动保存
```

如果 NG：

```text
测试完成

362.0 ℃

✕ NG

已自动保存
```

---

# 十一、线别功能

App 必须支持：

- 线别列表
- 新增线别
- 修改线别
- 删除线别
- 搜索线别
- 下拉选择

例如：

```text
L01
L02
L03
L04
L05
```

---

# 十二、机种功能

App 必须支持：

- 机种列表
- 新增机种
- 修改机种
- 删除机种
- 搜索机种
- 下拉选择

而且下拉框必须支持：

**输入文字实时过滤。**

例如机种很多：

```text
ABC-001
ABC-002
ABC-003
ABC-100
ABC-101
XYZ-001
XYZ-002
```

输入：

```text
ABC-1
```

自动只显示匹配结果。

---

# 十三、配置中心

需要一个独立的“配置”页面。

至少包含：

## 仪器通信配置

```text
IP 地址
Port
Unit ID
连接超时时间
轮询周期
```

默认：

```text
Port = 502
Unit ID = 1
Poll Interval = 1000ms
```

这些配置必须持久化保存。

用户配置一次后，下次打开 App 自动读取。

---

# 十四、配置文件

我希望设计一个专门的配置机制。

至少可以保存：

```text
设备通信配置
线别列表
机种列表
```

例如可以设计成：

```text
config.json
```

或者 Android 本地数据库 + JSON 导入/导出。

你需要分析哪一种更适合。

最好支持：

```text
导出配置
导入配置
```

这样以后可以把一台平板配置好的内容复制到另一台平板。

但是：

**配置文件设计不能影响正常数据库存储。**

需要把“配置数据”和“测量历史数据”分开。

---

# 十五、测量数据

每一次测试成功后自动保存。

建议至少保存以下字段：

```text
id
timestamp
line
model
device_id
device_ip
set_temperature
measured_temperature
tolerance
result
```

例如：

```text
ID：000001

时间：
2026-09-07 22:31:05

线别：
L01

机种：
ABC-001

设备：
191AF+

设备 IP：
192.168.1.100

设定温度：
350℃

测量温度：
351℃

误差范围：
±10℃

结果：
OK
```

---

# 十六、数据库

需要设计 Android 本地数据库。

优先分析：

**Room + SQLite**

是否合适。

数据库至少应该包含：

### MeasurementRecord

```text
id
timestamp
lineId / lineName
modelId / modelName
deviceId
deviceIp
setTemperature
measuredTemperature
tolerance
result
```

### Line

```text
id
name
enabled
createdAt
updatedAt
```

### Model

```text
id
name
enabled
createdAt
updatedAt
```

### DeviceConfig

```text
id
name
ip
port
unitId
pollInterval
enabled
```

如果你认为还需要其他表，可以提出。

---

# 十七、历史记录

需要一个历史数据页面。

支持：

- 查看所有记录
- 按日期筛选
- 按线别筛选
- 按机种筛选
- 按 OK/NG 筛选
- 关键词搜索
- 查看详细记录

例如：

```text
2026-09-07

09:01:12
L01 / ABC-001
351℃ / OK

09:03:25
L01 / ABC-001
362℃ / NG

09:05:31
L01 / ABC-001
350℃ / OK
```

---

# 十八、Excel 导出

必须支持：

**导出 Excel 文件。**

例如：

```text
测量记录_20260907.xlsx
```

Excel 字段：

```text
ID
时间
线别
机种
设备
设备IP
设定温度
测量温度
误差范围
结果
```

最好支持导出前筛选：

```text
日期范围
线别
机种
结果
```

例如：

```text
2026/09/01 ～ 2026/09/07
线别：L01
机种：ABC-001
结果：全部
```

然后导出。

---

# 十九、通信模块设计

请把 MODBUS TCP 通信独立成一个模块。

不要把 Socket 代码直接写在 Activity / UI 中。

推荐架构：

```text
UI
 ↓
ViewModel
 ↓
Repository
 ↓
ModbusService
 ↓
TCP Socket
 ↓
191AF+
```

通信模块负责：

```text
connect()
disconnect()

readHoldingRegisters()
writeSingleRegister()
writeMultipleRegisters()

readRealtimeTemperature()
readUploadFlag()
readSavedTemperature()
readTestResult()
```

---

# 二十、轮询逻辑

核心逻辑：

```text
连接仪器
 ↓
每约 1 秒执行一次
 ↓
读取 0x1C
 ↓
0 → 无新测试
 ↓
1 → 有新测试
 ↓
读取 0x1D
 ↓
读取 0x1E
 ↓
创建 MeasurementRecord
 ↓
写入 Room
 ↓
更新 UI
```

需要考虑：

- TCP 断线
- Wi-Fi 暂时断开
- 仪器关机
- 仪器 IP 不可达
- Socket timeout
- 数据解析错误
- MODBUS exception response
- CRC 是否需要（注意：MODBUS TCP 不使用 RTU CRC）
- 读取失败重试
- App 从后台恢复
- Android 生命周期
- 防止重复保存

---

# 二十一、特别注意“重复保存”

因为：

```text
0x1C = 1
```

并且：

**读取以后自动清零。**

因此设计上需要避免：

```text
读取 0x1C = 1
读取 0x1D
读取 0x1E
数据库保存
```

过程中 App 崩溃或者网络异常造成重复/丢失。

需要分析：

**什么时候认为一次测试已经成功确认？**

并设计合理的事务/状态机。

---

# 二十二、请设计状态机

建议分析并设计类似：

```text
DISCONNECTED
      ↓
CONNECTING
      ↓
CONNECTED
      ↓
POLLING
      ↓
NEW_RESULT_DETECTED
      ↓
READING_RESULT
      ↓
SAVING
      ↓
SAVED
      ↓
POLLING
```

异常：

```text
CONNECTED
   ↓
NETWORK_ERROR
   ↓
RECONNECTING
   ↓
CONNECTED
```

请详细说明状态转换条件。

---

# 二十三、MODBUS TCP 报文

请根据厂家 PDF 给出的示例，设计实际 App 使用的请求。

厂家示例：

请求：

```text
00 01 00 00 00 06 01 03 00 00 00 0A
```

其中：

```text
Transaction ID = 0001
Protocol ID    = 0000
Length         = 0006
Unit ID        = 01
Function       = 03
Start Address  = 0000
Quantity       = 000A
```

响应示例：

```text
00 01 00 00 00 17 01 03 14 ...
```

厂家示例说明：

```text
寄存器 0 = 实时温度
例如：
00 EA
= 234
= 23.4℃
```

请根据这个规则设计：

### 读取 0x1C~0x1E

优先考虑一次读取：

```text
Start Address = 0x001C
Quantity = 3
```

即：

```text
01 03 00 1C 00 03
```

然后说明完整 MODBUS TCP MBAP Header 应该怎么组成。

不要只给结论，请解释字节结构。

---

# 二十四、数据类型和缩放

目前厂家文档说明：

实时温度：

```text
0x00
系数 0.1
```

例如：

```text
234 → 23.4℃
```

测试保存温度：

```text
0x1D
单位 ℃
```

例如：

```text
351 → 351℃
```

请严格区分：

```text
0x00 的缩放规则
0x1D 的缩放规则
```

不要把所有温度都默认乘 0.1。

---

# 二十五、网络配置

厂家提供了二维码配置方式。

文档中明确有：

```text
WifiName
WifiPassword
ServerIP
ServerPort
GateWay
```

例如：

```text
WifiName=Quick-service
WifiPassword=1921681711
ServerIP=192.168.170.100
ServerPort=1883
```

但是注意：

厂家文档特别说明：

**MQTT 和 MODBUS TCP 模式下 ServerIP 的含义不同。**

MQTT：

```text
ServerIP = 上位机 IP
```

MODBUS TCP：

```text
ServerIP = 测试仪 IP
```

本项目采用：

**MODBUS TCP**

所以不要把 MQTT 示例直接照搬。

请明确分析：

- 191AF+ 的 IP 应如何配置
- Android 平板如何连接同一个工厂 Wi-Fi
- Android App 如何连接仪器 IP:502
- DHCP / 静态 IP 的优缺点
- 工厂网络环境下可能存在的 AP isolation / VLAN / 防火墙问题

但不要改变项目当前目标：

**一台仪器 + 一台 Android 平板 + 工厂 Wi-Fi。**

---

# 二十六、UI/UX 原则

这是一个工业现场 App。

因此优先：

- 大字体
- 清晰状态
- 简单操作
- 高可读性
- 少按钮
- 明确 OK / NG
- 网络状态明显
- 测试完成明显
- 避免误操作

操作员的主要操作应该只有：

```text
选择线别
选择机种
进行测量
按仪器保存按钮
```

App 自动完成：

```text
读取
判断新数据
获取结果
保存
显示
```

---

# 二十七、推荐技术栈

请评估并给出最终建议。

默认优先考虑：

```text
Kotlin
Android
Jetpack Compose
MVVM
Coroutines
StateFlow
Room
Apache POI / Android 适合的 Excel 生成方案
```

但对于 Excel 导出，请认真评估：

- Apache POI
- FastExcel
- 其他 Android 兼容方案

选择一个最适合本项目的。

MODBUS TCP：

可以自行基于：

```text
java.net.Socket
```

实现简单 MODBUS TCP Client，

或者使用成熟库。

请比较：

**自己实现 vs 使用第三方 MODBUS 库**

并选择适合工业项目长期维护的方案。

---

# 二十八、请先不要马上写完整 App

你的第一阶段任务不是立即生成完整代码。

请先完成：

## Phase 1：协议分析

输出：

1. 当前两份 PDF 的协议总结
2. 191AF+ 已知寄存器表
3. 哪些寄存器是 App 必须读取的
4. MODBUS TCP 报文结构
5. 目前协议未知项
6. 必须向厂家确认的问题

---

# 二十九、Phase 2：通信验证工具

设计一个最小 Android 测试 App。

只实现：

```text
IP
Port
Unit ID

连接

实时读取：
0x00
0x1A
0x1B
0x1C
0x1D
0x1E

自动轮询

通信日志
```

日志例如：

```text
[22:31:01] CONNECT 192.168.1.100:502
[22:31:02] READ 0x001C~0x001E
[22:31:02] 0x1C=0
[22:31:03] READ 0x001C~0x001E
[22:31:03] 0x1C=1
[22:31:03] 0x1D=351
[22:31:03] 0x1E=1
[22:31:03] RESULT = OK
```

这个工具用于现场验证真实设备行为。

---

# 三十、Phase 3：正式 App

确认通信以后再实现：

```text
测量
历史记录
配置
线别管理
机种管理
数据库
Excel 导出
配置导入/导出
```

---

# 三十一、Phase 4：异常测试

必须测试：

### 网络

- Wi-Fi 断开
- Wi-Fi 恢复
- 仪器关机
- 仪器重新开机
- IP 错误
- Port 错误
- TCP timeout

### 测试流程

- 正常 OK
- 正常 NG
- 连续快速保存
- 长时间运行
- App 切后台
- App 恢复
- Android 平板重启

### 数据

- 数据重复
- 数据丢失
- 数据库写入失败
- Excel 导出失败
- 配置导入失败

---

# 三十二、必须重点分析的数据丢失风险

请认真分析：

假设：

```text
仪器：
0x1C = 1
0x1D = 351
0x1E = 1
```

App 读取 0x1C 后，仪器自动清零。

但此时：

```text
App 在读取 0x1D 前崩溃
```

会发生什么？

是否可能导致：

```text
0x1C 已清零
但 App 没有保存记录
```

如果存在这个风险，请明确指出。

并提出：

- 是否应该一次读取 `0x1C~0x1E`
- 是否应该一次 MODBUS 请求读取
- 是否应该先读取 0x1D/0x1E
- 如何减少失败窗口
- 是否需要本地 pending 状态
- 哪些问题必须让厂家确认

不要为了“看起来完整”而假装这个问题已经解决。

---

# 三十三、请考虑“自动获取结果”的可靠性

本项目的核心业务要求：

> 操作员只按仪器上的保存按钮，App 自动获取结果并保存。

因此请重点分析：

```text
仪器保存
↓
0x1C
↓
App Poll
↓
读取结果
↓
数据库
```

这个链路是否可靠。

如果厂家协议存在天然限制，要明确指出。

---

# 三十四、请设计最终系统架构

最终希望得到：

```text
┌─────────────────────────────┐
│         Android Tablet       │
│                             │
│  ┌───────────────────────┐  │
│  │       UI Layer        │  │
│  └──────────┬────────────┘  │
│             │               │
│  ┌──────────▼────────────┐  │
│  │      ViewModel        │  │
│  └──────────┬────────────┘  │
│             │               │
│  ┌──────────▼────────────┐  │
│  │      Repository       │  │
│  └───────┬────────┬──────┘  │
│          │        │         │
│  ┌───────▼───┐ ┌──▼──────┐  │
│  │ Modbus TCP│ │  Room   │  │
│  │  Service  │ │Database │  │
│  └───────┬───┘ └─────────┘  │
│          │                  │
└──────────┼──────────────────┘
           │
        Wi-Fi/TCP
           │
           ▼
      ┌───────────┐
      │  191AF+   │
      │ Measuring │
      │   Meter   │
      └───────────┘
```

---

# 三十五、最终请输出以下成果

请不要只给我一堆代码。

我要你按以下顺序完成：

## A. 项目可行性结论

明确回答：

**这个项目是否可以实现？**

重点回答：

> 测量完成、操作员按仪器保存按钮以后，Android App 是否可以自动得到结果？

---

## B. 协议分析

整理厂家 PDF 中确认的信息。

---

## C. 已知寄存器表

整理：

```text
地址
名称
数据类型
单位
缩放
读写
App用途
```

---

## D. 未知信息清单

列出所有不能从厂家 PDF 得出的信息。

---

## E. 厂家确认问题清单

帮我整理一份可以直接发给厂家的问题。

特别包括：

1. 191AF+ 是否确定支持 MODBUS TCP？
2. 默认 IP 获取方式？
3. 完整 Register Map？
4. 0x1C 的详细时序？
5. 0x1C 是否读取一次后立即清零？
6. 0x1D / 0x1E 在读取 0x1C 后是否仍保持有效？
7. 如果 App 未连接，仪器按保存按钮会不会丢数据？
8. 连续两次保存之间有没有最小时间间隔？
9. 0x1D 的单位和数据类型是否始终为整数 ℃？
10. 0x1E 的 OK/NG 是否完全由仪器最终判定？
11. 是否存在其他与测试结果相关的寄存器？
12. 是否支持静态 IP？
13. 是否支持 DHCP？
14. Wi-Fi 是否只支持 2.4GHz？
15. MODBUS TCP 默认 Unit ID 是否始终为 1？
16. 最大连接数是多少？
17. TCP 是否只允许一个客户端？
18. 通讯超时后的行为是什么？
19. 是否可以连续 1 秒轮询？
20. 如果设备进入省电/休眠状态，Wi-Fi/TCP 会如何处理？

---

# 三十六、不要做的事情

禁止：

1. 臆造寄存器。
2. 臆造厂家没有说明的数据结构。
3. 把 MQTT 协议和 MODBUS TCP 混淆。
4. 把 MODBUS RTU 和 MODBUS TCP 的帧格式混用。
5. 把 0x00 的 0.1 缩放规则错误应用到 0x1D。
6. 假设仪器会主动推送数据。
7. 假设 0x1C/0x1D/0x1E 的真实时序已经验证。
8. 在没有实际设备测试的情况下声称通信已经验证成功。
9. 一开始就生成大量无法验证的完整 App 代码。

---

# 三十七、重要开发原则

这是一个工业现场数据采集系统。

请优先考虑：

**可靠性 > 可维护性 > 简洁性 > UI 美观**

数据不能轻易丢失。

网络断开以后需要自动重连。

数据库必须本地保存。

App 不应该依赖云服务器才能正常工作。

Excel 导出应该使用本地数据。

配置应该本地持久化。

---

# 三十八、最终目标

最终我要得到一个 Android 平板 App：

```text
打开 App
   ↓
自动连接 191AF+
   ↓
选择线别
   ↓
选择机种
   ↓
开始工作
   ↓
操作员正常测量
   ↓
操作员按 191AF+ 保存按钮
   ↓
App 自动发现测试结果
   ↓
自动读取：
温度
OK/NG
   ↓
自动保存：
时间
线别
机种
设备
温度
结果
   ↓
历史记录可以查询
   ↓
最后可以筛选并导出 Excel
```

整个过程：

**操作员不需要在 App 上重复点击“保存”。**

---

# 三十九、你的第一步任务

现在请不要直接写完整 App。

先完成：

### 第一阶段：

**《191AF+ → Android App 技术可行性与通讯规格分析 V1.0》**

包括：

1. 可行性结论
2. 协议分析
3. 寄存器分析
4. MODBUS TCP 通讯流程
5. 自动获取结果流程
6. 数据库设计
7. App 架构
8. UI 页面规划
9. Excel 导出方案
10. 配置文件方案
11. 网络异常处理
12. 数据丢失风险
13. 通信验证方案
14. 厂家确认问题清单

完成第一阶段后，再进入：

**第二阶段：MODBUS TCP Android 通信测试工具设计与实现。**

然后才进入：

**第三阶段：正式生产 App。**

请始终记住：

**厂家 PDF 是协议事实来源；不确定的地方必须标记，不允许自行假设。**
---

# 四十、2026-09-21 新增需求：双测量界面（烙铁 192AF+ / 扭力计）

> 本章是**追加需求**，优先级高于前面各章中与之冲突的表述（尤其是「只有一个测量界面」的隐含前提）。
> 命名约定：**烙铁 = 原有的 192AF+ 测温仪界面（Wi-Fi / MODBUS TCP）；扭力计 = 新增的 USB 串口界面。**
> 以后讨论一律用这两个词区分。

## 40.0 一句话

App 从「单仪器单界面」升级为**双界面**：同一台平板、同一个 App，同时服务**两台仪器** ——
烙铁测温仪 192AF+（Wi-Fi）与扭力计（USB）。两个界面各自独立采集、独立配置、独立历史、独立导出，
**互不干扰**。

## 40.1 扭力计的硬件与连接方式【事实：厂家手册 6.2 资料输出】

| 项 | 内容 |
|---|---|
| 物理连接 | USB 线直连平板（插头 **USB B TYPE**），平板需支持 **USB Host / OTG** |
| 驱动类型 | **FTDI 虚拟串口**（手册原文指向 FTDI Virtual COM Port Drivers `http://www.ftdichip.com/Drivers/VCP.htm`） |
| 资料位元长度 | **1 个起始位 + 8 个数据位 + 2 个停止位 + 无校验位**（即 **8N2**） |
| 速率 | **19200 bps** |
| 输出格式 | 手册只写「ASCII 格式」，**帧格式细节未知** |
| 数据方向 | **设备主动推送**：扭力计测完一笔就把结果发给上位机（用户 2026-09-21 口述） |

> ⚠️ 手册那一节是按 **iPad** 写的（「設備跟IPAD進行USB連接」）。Android 侧做法不同（USB Host API + 串口驱动），
> 但**串口参数与数据格式是设备侧的**，与上位机是 iPad 还是 Android 无关。

## 40.2 六条硬需求（用户原话 → 落地方式）

| # | 用户原话 | 落地方式 |
|---|---|---|
| 1 | 「是要数据独立，扭力计测量跟烙铁测量的部分不管是**设置的数据**还是**保存导出的部分**都要相互独立，不能混在一起」 | **完全独立的表与流程**：扭力计自己的线别/机种/扭矩范围/设备信息/记录表；导出各自独立，互不串数据 |
| 2 | 「扭力计测量部分可以配置选择**线别 / 机种 / 扭矩范围 / 设备信息**，这里参考烙铁的部分」 | 测量页四个下拉（先在管理页预建），与烙铁的交互一致 |
| 3 | 「历史数据的部分可以参考烙铁的部分，导出 excel 先随便搞一些字段就行了，因为扭力计的具体内容格式我还没确定」 | 历史页交互照抄烙铁（日期范围/关键词/删除需密码）；导出字段先占位，格式定了再改 |
| 4 | 「设备信息的添加要加一个**可以调用后置摄像头来扫码**的功能」 | 管理页「设备信息」新增时，除手输外多一个「扫码」按钮 → 后置摄像头识别条码/二维码 → 回填 |
| 5 | 「保存数据是需要等**三次扭力计测量结果**出来后求平均值再自动（保存），**前面的三次测量结果也要保存**；目前依旧随意，也不用求平均值，毕竟现在不确定具体的数据结构」 | 一笔「测量会话」= 连续 3 笔读数：**每笔各存一条**（seq 1/2/3），第 3 笔到达时再自动存一条**平均值记录**（当前数据未定义 → 平均值先留空，结构定了再补算法） |
| 6 | 「删除历史记录的密码跟烙铁的一致即可」 | 复用同一套删除密码（不新建） |

## 40.3 界面与切换（**最关键的两条约束**）

> 「添加新界面**不能影响到原本烙铁的界面功能**，最重要的**切换界面时，烙铁的通信不能断**。」
> 「如果监测到扭力计界面有数据传入，就**自动切到扭力计界面**。」

设计结论：

1. **两个采集器都是 App 级常驻**（与界面无关）：
   - 烙铁：`MeasurementController` + 前台服务 + Modbus 轮询 —— **切界面时继续跑，连接不断**（这一条是硬要求）；
   - 扭力计：`TorqueController` + USB 串口监听 —— **只要插着设备就一直在听**，不依赖扭力计界面是否显示。
2. **界面切换只是切视图**，不启动/停止任何采集 → 天然满足「切界面不断通信」。
3. **扭力计来数据 → 自动切到扭力计界面**：由常驻监听触发一次界面跳转。
4. **不允许出现的回归**：烙铁的轮询周期、触发状态机、弹卡、入库、导出一律不因新增界面而改变。

## 40.4 数据独立性（对应第 1 条）

- 扭力计**不复用**烙铁的任何字典表与记录表 —— 哪怕字段看起来一样（线别/机种只是名字相同）。
- 原因：用户明确要求"不能混在一起"；且**烙铁侧的代码路径一行都不改**，是"不影响烙铁"最可靠的保证方式。
- 删除密码是唯一共用项（用户第 6 条明确要求一致）。

## 40.5 扭力计的数据结构【**未知，待 2026-09-22 实测** → ✅ 已于 2026-09-22 实测确认，见 §41.2】

用户暂时不给格式，明天实测后提供。在此之前：

- 实现一个**解析器接缝** `torque/TorqueParser.kt`：`ByteArray → TorqueReading?`，
  当前只做「收到字节流 → 原样记录」，不猜数值语义；
- **USB 诊断面板**：列出已连接 USB 设备的 VID/PID/接口类型 + 原始字节 hex 日志 + 一键复制
  —— 明天现场用它把真实数据取回来，再据此实现解析；
- 三条候选解析路径（**先不选，等真实数据**）：
  1. ASCII 文本行（如 `1.234\r\n`）——手册写「ASCII 格式」，可能性最高；
  2. 带前缀的 ASCII 帧（如 `T=1.234`）；
  3. 二进制小端/大端帧（若实测发现是二进制）。

> 明确记录：**在拿到真实数据之前，`TorqueReading` 的数值字段一律为空**，不编造。

> **2026-09-21 实施时的修订**（见 40.9）：这条被放宽为「**尽力解析 + 原文保底**」。
> 原方案（数值一律留空）会让现场拿到数据后**还得再改一次才能看到数字**，而按通用称重行
> （`ST,GS,+001.234,N.m` 一类）先解析、**同时把报文原文逐字节入库**（`rawLine`/`rawHex`），
> 两种做法都不丢数据，但后者明天一到现场就能看出「解析对了没有」。
> 单位仍然**不下结论**：界面一律显示「单位待确认」，`unitText` 只存报文里抽到的原文。

## 40.6 三次测量 → 平均值 → 自动保存（对应第 5 条）【⛔ 已被 §41.4 取代】

```text
扭力计测量第 1 笔 → 存一条（seq=1）→ 界面显示 1/3
扭力计测量第 2 笔 → 存一条（seq=2）→ 界面显示 2/3
扭力计测量第 3 笔 → 存一条（seq=3）→ 自动计算平均值 → 存一条（seq=0，isAverage）
                    ↑ 全程无需操作员在 App 上点任何按钮
```

- 三次计数**按会话**：一次会话结束（存完平均值）后，下一笔重新从 1 开始计。
- 数据格式未定义期间：**平均值字段先留空**（不猜），但会话机制、次数显示、自动保存的流程全部打通。
- 会话中断（如拔线、切界面）不丢已入库的前几笔。

## 40.7 本阶段不做 / 不猜

1. 不猜扭力计的帧格式、不猜单位（N·m / kgf·cm / lbf·in 未定）、不猜小数位。
2. 不做扭力计的 OK/NG 判定（用户未要求，且判据未知）。
3. 不改烙铁的寄存器表、触发逻辑、弹卡行为、导出列。

## 40.8 待确认清单（已由用户逐条拍板）

| 问题 | 用户决定 |
|---|---|
| 两个界面的**入口形式** | **底部常驻切换条**（只在两个测量主页显示，子页面不显示） |
| 扭力计的线别/机种 | **完全另建一份**（四类字典 `torque_dict`，不复用烙铁表） |
| **191AF+ → 192AF+ 改名的范围** | **App 界面 + 文档注释**；备份格式标识 `quik-191af-collector` **不动** |
| 扭力计到数的**界面反馈** | **自动切页 + 页内显示，第 3 笔弹卡 + 振动** |
| USB 驱动方案 | **用 usb-serial-for-android 库** —— ⚠️ **没能照做**，见 40.9 第 1 条 |
| 设备信息选择项是否清空 | ~~保存后不清空~~ **2026-09-22 用户修正：保存成功后清空**（原话：「设备信息其实是电动螺丝机的 ID，保存完平均值后清空」—— 与烙铁「存一笔清一次设备编号」同一习惯） |

## 40.9 实施结果（2026-09-21，DB v8）

**已实现**：四张字典 + 测量/记录/管理/诊断四个页面 + 扫码头 + 三次一组自动平均 + 自动切页 + 独立导出。
单测从 42 例增至 **80 例（0 失败）**，`assembleDebug` 通过。详细规格见
`docs/可行性分析与通讯规格-V1.0.md` **§24**。

**必须回报用户的偏离**：

1. **USB 库没能用官方库**：`com.github.mik3y:usb-serial-for-android:3.8.1` 只发布在 JitPack，
   构建期实测 jitpack.io 与国内各镜像**全部取不到**（404/401/连接失败）→ 改为**自写最小 FTDI 驱动**
   （`torque/usb/`，只覆盖 FT232R 一族 19200 8N2）。替换回官方库只需改 `UsbSerialLink` + `FtdiSerialPort` 两个类。
2. **40.5 的「数值一律留空」放宽为「尽力解析 + 原文保底」**（理由见 40.5 下的修订说明）。
3. **自动切页只从烙铁测量页触发**：人正待在历史/配置/管理/诊断页时不抢；手动切页后 5 秒内不抢。

**未验证**（等 2026-09-22 真机）：报文格式、芯片型号是否 FT232R、19200/8N2/分频 `0x809C`、
DB v7→v8 实机迁移、切页时烙铁不断线、单位与判定语语义。清单见 README「扭力计真机验证清单」。

---

## 41. 2026-09-22 第二轮：报文格式实测确认 + 七条修改要求

### 41.0 一句话

用户用串口助手测出了真实报文（`+5.40kgf*cm`，**没有换行符**），并明确指出「**你之前把问题复杂化了，
本来就是接收一下 ASCII 码就行了的事情**」—— 于是解析从「按行读 + 通用称重行猜测」改成
**在缓冲区里扫 `(符号)(数字)(单位)`**；同时按七条要求把扭力计这一侧从「先能跑」改成「现场能直接交付」。

### 41.1 七条要求（用户原话 → 落地处）

| # | 用户要求 | 落地 |
|---|---|---|
| 1 | 「界面切换按键太下面了/太矮了，跟手机/IPAD 的导航按钮重叠了」 | `ui/App.kt`：切换条自己吃 `navigationBarsPadding()`、高度 88 dp（见 §41.7） |
| 2 | 格式确认为 `+5.40kgf*cm`；单位单独标注；**三次结果分开显示暂存**；要有**重测按键（无需密码）**；**测满三次后 5 秒内没有重测就自动保存**（时间可设定，默认 5 秒） | `TorqueParser`（扫描式解析）、`TorqueSession`（暂存/重测/延时）、`TorqueController`（倒计时与自动保存）、测量页三个暂存格 + 倒计时 + 重测按钮（§41.4） |
| 3 | 简化扭力计 UI，删掉没必要的说明，**通信记录搬到诊断页**（参照烙铁） | 测量页只留链路状态/四下拉/暂存/判定/重测/倒计时/上一条；原始字节与操作日志只在诊断页（§41.7） |
| 4 | 导出 Excel 必须有：时间、线别、机种、扭矩范围、设备信息、**三次扭矩测量结果**、平均扭矩、判断结果 | `TorqueXlsxExporter` 16 列、**一行一组**（§41.8） |
| 5 | 新增扭矩范围要填**上限 + 下限**两个选项，填写时标注单位 `kgf*cm` | `TorqueDict.minValue/maxValue` + 管理页双输入框 + 名称自动生成 + 单位标注（§41.3） |
| 6 | 结果按**平均值**判断：落在所选范围内 OK，否则 NG | `torqueJudge(avg, min, max)`，闭区间（§41.6） |
| 7 | 最后检查配置数据的导出/导入是否正常 | 备份 v6 + **往返单测** `ConfigBackupTorqueTest`（10 例，§41.8） |

### 41.2 实测确认的报文格式（FACT）

用户 2026-09-22 用串口助手实测（配置：**19200 / 8 数据位 / 2 停止位 / 无校验 / 无流控**）：

| 项 | 实测值 | 备注 |
|---|---|---|
| 报文形状 | `+5.40kgf*cm` | 符号 + 2 位小数 + 单位 |
| 换行符 | **没有** | 串口助手按 ~50 ms 间隔把连续字节**分包显示**，报文之间没有 `\r\n` |
| 单位 | `kgf*cm` | 设备原样输出（`*` 不改成 `·`） |
| 设备行为 | **主动输出**，App 不发任何指令 | 手册 6.2「资料输出」|

**因此解析规则（`TorqueParser`）**：

1. 收到的字节全部进缓冲区，用正则扫 `(符号)?(数字)(单位)`，单位表长写在前
   （`kgf*cm` / `mN*m` / `N*m` …，分隔符允许 `*` `.` `-` `·`）；
2. **不按行读、不依赖任何分隔符** —— 读数之间靠内容区分，这是「没有换行符」的直接后果；
3. 一次读取里出现多个读数 → **全部取出**（不能只取第一个）；
4. 一个读数被拆成两包 → 靠缓冲区拼起来（不能漏读）；
5. 没配成读数的内容留在 `pendingText`，诊断页「解析缓冲」原样显示，并给出**丢弃字节数**
   （不静默丢字节）；
6. 每笔仍存**报文原文 + 逐字节 hex**（`rawText` / `rawHex`），格式若再变可回溯重放。

> 明确记录：**这一轮的格式是用户实测提供的**，不是推测；但「真机 USB 链路能否把这串字节完整读出来」
> 仍未验证（自写 FTDI 驱动，见 §41.11）。

### 41.3 数据模型 v9（DB v8 → v9）

- `torque_dict` **加两列**：`minValue` / `maxValue`（REAL，可空，kgf*cm，仅 `kind = range` 有值）。
  纯 `ALTER TABLE ADD COLUMN`，老字典一条不丢。
- `torque_record` **改成「一组一行」**：`v1` / `v2` / `v3` / `average` / `judge` / `rangeMin` / `rangeMax`
  （SQLite 改不了列 → **重建表并按 `sessionId` 聚合迁移**：三个单笔还原到三列、平均行还原到 `average`；
  判据旧数据里没有 → 上下限留 NULL、`judge` 留空，界面显示「未判定」，**不编造**）。
- 迁移 SQL 必须与 Room 由数据类生成的建表语句**逐字一致**（否则升级装与全新装的 schema 不一致）。
- **烙铁的表一个字段都没动。**

### 41.4 一次测量组 = 库里一行（取代 §40.6 的「一笔一行 + 一行平均」）

```text
第 1 笔 → 暂存(1/3)   第 2 笔 → 暂存(2/3)   第 3 笔 → 暂存(3/3) → 起延时（默认 5 秒，可设）
   ├─ 延时内点「重测」 → 三笔全清，从 1/3 重新开始（无需密码）
   └─ 延时到 且 四项信息齐全 → 自动保存成一行（三笔 + 平均 + OK/NG）
```

1. **暂存**：三笔只在内存（`TorqueSession`），未保存的组**在库里不存在** —— 代价是「正在测的这一组」
   重启 App 会丢（未保存的本来就不是记录），已保存的一条都不会少；
2. **延时默认 5000 ms**，诊断页可选 3/5/10/15/30 秒，落 `app_setting`（`torque.saveDelayMs`）；
3. **第 4 笔及以后**（满组后、延时内又来）→ **忽略 + 递增计数 + 页面写明**，
   **不并入本组**、不影响本组照常保存（用户拍板）；界面把「已忽略 N 笔」显示出来（不静默丢弃）；
4. **「时间」取第三笔的时刻**（用户拍板）；`rangeMin/rangeMax` 是**判定当时的上下限快照** ——
   范围字典以后被改了，这条记录仍能自证「当时凭什么判 OK」；
5. **重测**：清空暂存 + 取消倒计时（`sessionSeq` 递增，防止旧读数串到新组）；
6. **保存前不清空**暂存（界面在保存前一刻不能看到空的），保存成功才 `reset()`；
7. 保存成功后**「设备信息」下拉自动清空**（用户 2026-09-22 修正，见 §40.8 修订行）。

### 41.5 四项信息硬门槛（用户硬要求，原文）

> 「线别/机种/扭矩范围/设备信息，任何一个没选择，哪怕测量完成三次也不能保存，要有一栏显眼的提示信息
> 告知那些数据没选择（仅三次测完且信息不全的情况，这种情形下，未选的信息要字体也要变得明显，选择后回复正常）」

落地：

- 保存前设**三道门**：组满 → 延时已过 → **四项齐全**；缺任一项**不保存**，该组继续留在暂存里；
- 顶部橙色提示条：`三次已测完，但还不能保存！` + `请选择：…` + `这三笔结果仍在暂存里（不会丢）`；
- **缺的那一栏下拉框变橙**：边框加粗变橙、标签橙色加粗并带 `⚠` 前缀；**选上即恢复正常**；
- **补齐后立即保存**（不等下一次延时）—— 延时已过，四项一齐就落库；
- 提示条**只在「三次已测完且信息不全」时出现**，平时完全不打扰。

### 41.6 判定规则（用户要求 6）

```kotlin
fun torqueJudge(average: Double?, min: Double?, max: Double?): String? =
    if (average == null || min == null || max == null) null
    else if (average >= min && average <= max) "OK" else "NG"
```

- **先圆整到 2 位小数再判定**（平均值与设备输出同为 2 位）—— 「屏幕上看到的数」与
  「屏幕上看到的 OK/NG」永远一致，界面用同一个函数即时预判，入库也用同一个函数；
- **闭区间**（等于上下限算 OK）：现场按标准值卡边界时，判成不合格会引起不必要的返工；
- **缺任何一个值 → 不判定**（不编造）。范围字典是 v8 时代建的（没有上下限）→ **不保存**，
  提示「缺少上下限，请到管理页补填」，管理页对应条目标「⚠ 缺上下限」；
- **NG 也入库**（与烙铁「只保留 OK」相反）：这是业务规则差异 —— 扭矩不合格品同样要留证。

### 41.7 界面调整

**① 底部切换条被系统导航栏压住（用户要求 1）**

- 根因：`MainActivity` 调了 `enableEdgeToEdge()`，而切换条与内容区是**并排的两个兄弟**，
  两者都没吃导航栏内边距 → 切换条被画到系统导航栏底下（「太矮了、跟导航按钮重叠」）。
- 定则：**谁在最下面，谁负责导航栏内边距**。切换条自己 `navigationBarsPadding()` +
  高度 `88.dp`（= 88 dp + 导航栏高度）；内容区在**有切换条的页面**不再重复加。

**② 扭力计测量页简化（用户要求 3）**

- 删掉大段说明文字；测量页只留：链路状态 + 监听开关、四个下拉 + 扫码、三个暂存格 + 平均值 + 即时判定、
  倒计时、重测按钮、上一条已保存记录、缺项提示条；
- **通信记录（原始字节 / 操作日志）只在诊断页**，测量页不再铺（操作员看数字，调试的人进诊断）；
- 诊断页新增：**解析缓冲**（未识别内容 + 丢弃字节数）、**自动保存延时**选择（3/5/10/15/30 秒）、
  「待补记 N 组」。

**③ 记录页**：原来的「只看三次平均」改成**判定筛选**（全部 / 仅 OK / 仅 NG），列表按组显示（判定 + 平均值 + 三笔）。
**④ 弹卡**：OK 绿 / NG 红，显示平均值、三笔与范围边界；单笔到数只切页不弹卡（还要接着测第 2、3 笔）。

### 41.8 导出 16 列 + 配置备份 v6（用户要求 4、7）

`TorqueXlsxExporter.HEADERS`（一次一组 = 一行）：

```
ID / 时间 / 线别 / 机种 / 扭矩范围 / 设备信息 / 扭矩下限(kgf*cm) / 扭矩上限(kgf*cm) /
第1次(kgf*cm) / 第2次(kgf*cm) / 第3次(kgf*cm) / 平均扭矩(kgf*cm) / 判断结果 / 参与平均笔数 /
原始报文 / 原始字节(hex)
```

要求 4 点名的字段全部在内；数值固定 2 位小数；旧的「单位待确认」字样**全部去掉**（单位已实测确认）。

`ConfigBackup` version **6**：扭力计四类字典随配置一起导出/导入，扭矩范围**带上限/下限两个数**。
导入净化规则抽成**纯函数** `torqueDictsToJson` / `torqueDictsFromJson`（不依赖 Room，可单测）：

1. 只认四个已知 `kind`（外部文件写别的 kind 一律丢弃）；
2. 名称为空的一律丢弃；
3. 扭矩范围**两个数齐全且上限 ≥ 下限** → 名称**由两个数重算**（即使有人在备份里手改了名字，
   也不会出现「名字写 0.5~6、判据却是 1~5」这种对不上的情况）；
4. 数值不可用（v5 及更早的备份 / 反向量程）→ **保留名称、数值留空**，管理页标「⚠ 缺上下限」，
   操作员补填后即可判定 —— **不编造数值**；
5. 数字被写成字符串（手改备份常见）→ 按数字读，不整条丢掉；
6. 格式标识 `quik-191af-collector` **不动**（改了现场既有备份全部导入失败），并有**源码守卫单测**。

### 41.9 用户拍板的四个问题（AskUserQuestion）

| 问题 | 用户决定 |
|---|---|
| 扭矩范围名称 | **由上下限自动生成**（与温度设置/上限设置同一套做法，操作员不用打字） |
| 满三笔后延时内又来第 4 笔 | **忽略 + 页面写明** |
| 记录/导出的「时间」 | **第三笔的时间** |
| 「设备信息」的含义与清空 | = **电动螺丝机的 ID**；**保存完平均值后清空** |

### 41.10 测试与构建结果

- 单测 **111 例 / 0 失败**（上一轮 80 例）：新增 `TorqueDataTest`（13 例，口径）、`ConfigBackupTorqueTest`
  （10 例，备份往返）、`TorqueParserTest` 重写为按实测格式（16 例）、`TorqueSessionTest` 重写（12 例）；
- `assembleDebug` 通过，产物 `app/build/outputs/apk/debug/app-debug.apk`；
- 单测里用**真正的 `org.json`**（`testImplementation("org.json:json:20231013")`，只进单测不进 APK）——
  否则「导入导出」这条根本测不了（Android 的 `org.json` 是桩实现，JVM 单测里全是 `not mocked`）。

### 41.11 仍未验证（诚实清单，真机逐条核对见 README）

1. **自写 FTDI 驱动在真机上能否跑通**（识别 → 配置 → 读到字节）——这一轮**一行都没在真机上跑过**；
2. **DB v8 → v9 的实机迁移**（聚合重建表）只在单测层面推演过；
3. **平板上的底部切换条观感**（导航栏是否真的不再遮挡）；
4. 扭力计 19200 8N2 的实际读数是否与设备屏幕一致；
5. 真机上「5 秒自动保存」与「重测」的实际手感（延时是否合适，诊断页可调）；
6. 四项拦截在真机上的提示是否足够显眼。
